www:
  type: python
  config:
    python_version: v2.7
    uwsgi_enable_threads: true
mailer:
  type: smtp
  config:
    smtp_relay_server: smtp.gmail.com
    smtp_relay_port: 587
    smtp_relay_username: 
    smtp_relay_password:
db:
  type: postgresql
cache:
  type: redis